Feature: Command Line | --eval

Example: --eval evaluates the given expression
    When running clox with "--eval '1 + 2'"
    Then clox prints to stdout:
    ```
    3
    ```

Example: When no expression is given
    When running clox with "--eval"
    Then clox fails with:
    ```
    Specify expression to evaluate after --eval

    Usage: clox [FLAGS]
    Usage: clox [FLAGS] --repl
    Usage: clox [FLAGS] --eval "expression"
    Usage: clox [FLAGS] --file path/to/file
    
    Flags:
        --bytecode     Print generated bytecode
        --help         Print this message
        --memory       Show contents of the clox-heap while tracing
        --no-run       Do not run the program
        --trace        Show execution of each instruction and state of the stack
    ```

Example: When the expression contains a compile error
    When running clox with "--eval '1 +'"
    Then clox fails with:
    ```
    [line 1] Error at end: Expect expression
    ```

Example: When the expression contains a runtime error
    When running clox with "--eval '-true'"
    Then clox fails with:
    ```
    Operand must be a number
    [line 1] in script
    ```